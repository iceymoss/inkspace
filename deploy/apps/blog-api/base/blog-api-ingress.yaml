# 文件路径: blog-api/base/blog-api-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog-api-ingress
  namespace: inkspace-backend
  labels:
    app.kubernetes.io/name: blog-api
    project: inkspace
  annotations:
    # 确保 Traefik Ingress Controller 识别
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - http:
        paths:
          # 默认使用 /inkspace 作为公共前缀，具体是否暴露为根路径 (/)
          # 或保留 /inkspace，由各环境的 overlays/ 中通过补丁文件决定。
          - path: /
            pathType: Prefix
            backend:
              service:
                name: blog-api # 🎯 目标 Service (ClusterIP)
                port:
                  number: 80 # Service 暴露的端口