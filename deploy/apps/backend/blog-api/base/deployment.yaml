apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-api
  labels:
    app.kubernetes.io/name: blog-api
    app.kubernetes.io/instance: blog-api
    project: inkspace
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: blog-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blog-api
    spec:
      # imagePullSecrets: # 如果是公开镜像不需要这个，保留结构供参考
      #   - name: registry-secret
      containers:
        - name: blog-api-container
          # ！！！ 关键：这是占位符，不是实际地址
          image: inkspace-blog-api-image
          ports:
            - containerPort: 8080
              name: http-blog-api
          resources:
            # 适配 1G 内存节点的 Base 配置
            limits:
              memory: "128Mi"
              cpu: "200m"
            requests:
              memory: "64Mi"
              cpu: "20m"