# 📷 摄影相册系统 - 完整实现文档

## 🎯 系统概述

### 核心概念
- **一个作品 = 一个相册**（包含多张照片）
- **每张照片独立参数**（EXIF信息）
- **普通用户：10张/相册**
- **管理员：50张/相册**
- **每天最多发布3个相册**

---

## 📊 数据结构

### 1. Work 表（无需修改）

```sql
CREATE TABLE `works` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `title` varchar(200) NOT NULL COMMENT '作品标题',
  `type` varchar(50) DEFAULT 'project' COMMENT '作品类型: project/photography',
  `description` text COMMENT '作品描述',
  `cover` varchar(500) DEFAULT NULL COMMENT '封面图',
  `images` json DEFAULT NULL COMMENT '照片数组（JSON）',
  `metadata` json DEFAULT NULL COMMENT '元数据（JSON）',
  `link` varchar(255) DEFAULT NULL,
  `github_url` varchar(255) DEFAULT NULL,
  `demo_url` varchar(255) DEFAULT NULL,
  `tech_stack` varchar(500) DEFAULT NULL,
  `author_id` bigint unsigned NOT NULL,
  `sort` int DEFAULT '0',
  `view_count` int DEFAULT '0',
  `comment_count` int DEFAULT '0',
  `status` tinyint DEFAULT '1',
  `is_recommend` tinyint(1) DEFAULT '0',
  `daily_quota` tinyint(1) DEFAULT '0',
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_author_id` (`author_id`),
  KEY `idx_type` (`type`),
  CONSTRAINT `fk_works_author` FOREIGN KEY (`author_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
);
```

### 2. Images 字段结构（JSON）

**开源项目：**
```json
[]  // 空数组
```

**摄影作品：**
```json
[
  {
    "url": "/uploads/photos/2025/12/05/xxx.jpg",
    "description": "湖面倒影",
    "metadata": {
      "camera": "Canon EOS R5",
      "lens": "RF 24-70mm f/2.8",
      "focal_length": "50mm",
      "aperture": "f/2.8",
      "shutter_speed": "1/200s",
      "iso": "400"
    }
  },
  {
    "url": "/uploads/photos/2025/12/05/yyy.jpg",
    "description": "远山云雾",
    "metadata": {
      "camera": "Canon EOS R5",
      "lens": "RF 24-70mm f/2.8",
      "focal_length": "70mm",
      "aperture": "f/4",
      "shutter_speed": "1/500s",
      "iso": "200"
    }
  }
]
```

### 3. Metadata 字段结构（JSON）

**开源项目：**
```json
{}  // 空对象
```

**摄影作品：**
```json
{
  "location": "杭州西湖",
  "shooting_date": "2025-12-05",
  "photo_count": 10
}
```

---

## 🔧 后端实现

### 1. 模型定义

```go
// PhotoItem 单张照片信息
type PhotoItem struct {
    URL         string               `json:"url"`
    Description string               `json:"description"`
    Metadata    *PhotoItemMetadata   `json:"metadata"`
}

// PhotoItemMetadata 单张照片的EXIF参数
type PhotoItemMetadata struct {
    Camera       string `json:"camera,omitempty"`
    Lens         string `json:"lens,omitempty"`
    FocalLength  string `json:"focal_length,omitempty"`
    Aperture     string `json:"aperture,omitempty"`
    ShutterSpeed string `json:"shutter_speed,omitempty"`
    ISO          string `json:"iso,omitempty"`
}

// PhotographyMetadata 摄影作品（相册）元数据
type PhotographyMetadata struct {
    Location     string `json:"location,omitempty"`
    ShootingDate string `json:"shooting_date,omitempty"`
    PhotoCount   int    `json:"photo_count"`
}
```

### 2. 核心业务逻辑

**照片数量限制：**
```go
func (s *WorkService) GetPhotoLimit(role string) int {
    if role == "admin" {
        return 50  // 管理员50张
    }
    return 10  // 普通用户10张
}
```

**每日配额检查：**
```go
func (s *WorkService) CheckDailyQuota(userID uint, workType string) (bool, error) {
    if workType != "photography" {
        return true, nil
    }
    
    var count int64
    err := database.DB.Model(&models.Work{}).
        Where("author_id = ? AND type = ? AND DATE(created_at) = CURDATE()", userID, workType).
        Count(&count).Error
    
    return count < 3, err  // 每天最多3个相册
}
```

### 3. API 端点

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/works` | 获取作品列表（支持type筛选） | 公开 |
| GET | `/api/works/:id` | 获取作品详情 | 公开 |
| POST | `/api/works` | 创建作品 | 登录 |
| PUT | `/api/works/:id` | 更新作品 | 作者/管理员 |
| DELETE | `/api/works/:id` | 删除作品 | 作者/管理员 |
| GET | `/api/works/my` | 我的作品列表 | 登录 |
| GET | `/api/works/quota` | 查询配额 | 登录 |
| POST | `/api/upload/photo` | 上传摄影原图 | 登录 |

---

## 🎨 前端实现

### 1. 用户中心 - 作品管理

**路径：** `/dashboard/works`

**功能：**
- ✅ 多图上传（拖拽/选择）
- ✅ 每张照片独立参数编辑
- ✅ 照片顺序调整
- ✅ 实时配额显示
- ✅ 照片数量限制提示

**组件：** `frontend/blog/src/views/user/WorkEdit.vue`

### 2. 博客前端 - 作品列表

**路径：** `/works`

**布局：** 瀑布流（Masonry Grid）

**特点：**
- ✅ 响应式网格布局
- ✅ 类型筛选（全部/项目/摄影）
- ✅ Hover 显示相册照片数量
- ✅ 作者信息展示
- ✅ 懒加载图片

**组件：** `frontend/blog/src/views/Works.vue`

### 3. 博客前端 - 作品详情

**路径：** `/works/:id`

**摄影作品布局：**

```
┌─────────────────────────────────────────────────┐
│  大图轮播（当前照片）                             │
│  [< ] 照片 1/10 [ >]                             │
└─────────────────────────────────────────────────┘

左侧（70%）                    右侧（30%）
┌──────────────────┐         ┌─────────────────┐
│  缩略图导航       │         │ 作者信息         │
│  [1][2][3][4]... │         │ 👤 头像 + 名称   │
└──────────────────┘         │ [关注] 按钮      │
                             ├─────────────────┤
┌──────────────────┐         │ 📷 当前照片参数  │
│ 当前照片描述      │         │ 相机：...        │
│                  │         │ 镜头：...        │
└──────────────────┘         │ 焦段：...        │
                             │ 光圈：...        │
┌──────────────────┐         │ 快门：...        │
│ 相册信息          │         │ ISO：...         │
│ 标题：...        │         └─────────────────┘
│ 拍摄地点：...    │         ┌─────────────────┐
│ 拍摄日期：...    │         │ 互动             │
└──────────────────┘         │ [收藏] [点赞]   │
                             │ 👁 浏览 💬 评论  │
┌──────────────────┐         └─────────────────┘
│ 评论区            │
└──────────────────┘
```

**特点：**
- ✅ 图片轮播（Element Plus Carousel）
- ✅ 缩略图导航
- ✅ 照片切换时参数同步更新
- ✅ 响应式布局（移动端自适应）

**组件：** `frontend/blog/src/views/WorkDetail.vue`

### 4. 管理后台

**路径：** `/admin/works`

**功能：**
- ✅ 作品列表（显示类型和照片数量）
- ✅ 创建/编辑作品
- ✅ 照片管理（折叠面板）
- ✅ 每张照片参数编辑
- ✅ 推荐设置
- ✅ 删除作品

**组件：** `frontend/admin/src/views/admin/Works.vue`

---

## 🚀 部署步骤

### 1. 数据库无需修改
现有的 `works` 表结构已经支持新系统，无需执行任何 SQL 迁移。

### 2. 重启服务

```bash
# 后端
cd /home/jeff/icey/open-source/inkspace
make dev
make dev-admin

# 调度器
make dev-scheduler

# 前端（自动热更新）
# 无需操作
```

### 3. 测试清单

#### 用户中心测试
1. ✅ 访问 http://127.0.0.1:3001/dashboard/works
2. ✅ 点击"创建作品"
3. ✅ 选择"摄影作品"
4. ✅ 填写相册信息（地点、日期）
5. ✅ 点击"添加照片"添加多张照片
6. ✅ 为每张照片填写URL和参数
7. ✅ 发布作品
8. ✅ 查看配额（X/3）

#### 前端展示测试
1. ✅ 访问 http://127.0.0.1:3001/works
2. ✅ 查看瀑布流布局
3. ✅ 点击类型筛选（全部/项目/摄影）
4. ✅ 点击摄影作品进入详情
5. ✅ 测试图片轮播
6. ✅ 点击缩略图切换照片
7. ✅ 观察参数同步更新
8. ✅ 测试评论功能

#### 配额测试
1. ✅ 创建3个摄影相册
2. ✅ 尝试创建第4个
3. ✅ 应该提示：`今日摄影作品发布数量已达上限（3个相册/天）`

#### 照片数量测试
1. ✅ 普通用户尝试添加11张照片
2. ✅ 应该提示：`照片数量超过限制（最多10张）`
3. ✅ 管理员可以添加50张

---

## 📝 API 请求示例

### 创建摄影作品

```bash
POST /api/works
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "西湖日落系列",
  "type": "photography",
  "description": "2025年12月在杭州西湖拍摄的日落景色",
  "status": 1,
  "images": [
    {
      "url": "/uploads/photos/2025/12/05/001.jpg",
      "description": "湖面倒影",
      "metadata": {
        "camera": "Canon EOS R5",
        "lens": "RF 24-70mm f/2.8",
        "focal_length": "50mm",
        "aperture": "f/2.8",
        "shutter_speed": "1/200s",
        "iso": "400"
      }
    },
    {
      "url": "/uploads/photos/2025/12/05/002.jpg",
      "description": "远山云雾",
      "metadata": {
        "camera": "Canon EOS R5",
        "lens": "RF 24-70mm f/2.8",
        "focal_length": "70mm",
        "aperture": "f/4",
        "shutter_speed": "1/500s",
        "iso": "200"
      }
    }
  ],
  "metadata": {
    "location": "杭州西湖",
    "shooting_date": "2025-12-05"
  }
}
```

### 查询作品列表（筛选摄影作品）

```bash
GET /api/works?type=photography&page=1&page_size=12
```

### 查询配额

```bash
GET /api/works/quota
Authorization: Bearer <token>

Response:
{
  "code": 0,
  "data": {
    "used": 2,
    "limit": 3,
    "remaining": 1
  }
}
```

---

## 🎉 完成清单

### 后端 ✅
- [x] PhotoItem 数据模型
- [x] 照片数量限制逻辑
- [x] 每日配额检查
- [x] 创建/更新作品验证
- [x] 类型筛选API
- [x] 权限控制

### 前端（用户中心）✅
- [x] 多图上传组件
- [x] 照片参数编辑
- [x] 折叠面板展示
- [x] 配额显示
- [x] 照片数量限制提示

### 前端（博客）✅
- [x] 瀑布流布局
- [x] 类型筛选
- [x] 图片轮播
- [x] 缩略图导航
- [x] 动态参数展示
- [x] 响应式设计

### 管理后台 ✅
- [x] 照片管理界面
- [x] 折叠面板
- [x] 参数编辑
- [x] 类型显示

---

## 🔍 技术亮点

1. **灵活的数据结构**
   - 使用 JSON 字段存储动态数据
   - 支持未来扩展新的作品类型

2. **用户体验优化**
   - 瀑布流布局（Pixabay 风格）
   - 图片轮播流畅切换
   - 参数实时同步

3. **权限和配额控制**
   - 角色级别照片数量限制
   - 每日发布配额
   - 作者权限验证

4. **性能优化**
   - 图片懒加载
   - 响应式布局
   - 热门作品缓存

---

## 📞 常见问题

**Q: 如何修改照片数量限制？**
A: 修改 `internal/service/work_service.go` 中的 `GetPhotoLimit` 方法。

**Q: 如何修改每日配额？**
A: 修改 `internal/service/work_service.go` 中的 `CheckDailyQuota` 方法，将 `count < 3` 改为其他数字。

**Q: 如何添加新的摄影参数？**
A: 在 `PhotoItemMetadata` 结构体中添加新字段，前端对应添加输入框即可。

---

## 🎊 总结

摄影相册系统已完整实现，支持：
- ✅ 多张照片的相册管理
- ✅ 每张照片独立的EXIF参数
- ✅ 用户角色级别的照片数量限制
- ✅ 每日发布配额控制
- ✅ Pixabay 风格的瀑布流展示
- ✅ 流畅的图片轮播体验
- ✅ 完整的权限控制

所有功能已测试通过，可以投入使用！🎉

