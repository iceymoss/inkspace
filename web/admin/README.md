# 管理后台前端

## 🎯 项目说明

这是系统管理后台的独立前端项目，与博客前端完全隔离。

---

## 🚀 启动

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

访问：http://localhost:3002

---

## 🔌 后端服务

管理后台前端连接到管理后台服务：

- **服务端口**: 8083
- **API地址**: http://localhost:8083/api
- **通过vite proxy**: `/api` → `http://localhost:8083/api`

---

## 📝 配置

### vite.config.js
```javascript
server: {
  port: 3002,  // 管理前端端口
  proxy: {
    '/api': {
      target: 'http://localhost:8083',  // 管理服务
      changeOrigin: true
    }
  }
}
```

---

## 🔐 登录

- **登录地址**: http://localhost:3002/login
- **默认账号**: admin / admin123

---

## 📁 项目结构

```
admin/
├── src/
│   ├── views/
│   │   └── admin/        # 只包含管理页面
│   ├── layouts/
│   │   └── AdminLayout.vue  # 只包含管理布局
│   ├── stores/
│   │   └── admin.js      # 只包含admin store
│   └── utils/
│       └── adminApi.js   # 管理API工具
├── vite.config.js
└── package.json
```

---

## ✨ 特点

- ✅ 完全独立的前端项目
- ✅ 只包含管理相关功能
- ✅ 独立的端口 (3002)
- ✅ 独立的token管理
- ✅ 与博客前端零耦合
