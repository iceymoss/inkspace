# 博客前端

## 🎯 项目说明

这是用户博客系统的独立前端项目，与管理后台完全隔离。

---

## 🚀 启动

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

访问：http://localhost:3001

---

## 🔌 后端服务

博客前端连接到用户服务：

- **服务端口**: 8081
- **API地址**: http://localhost:8081/api
- **通过vite proxy**: `/api` → `http://localhost:8081/api`

---

## 📝 配置

### vite.config.js
```javascript
server: {
  port: 3001,  // 博客前端端口
  proxy: {
    '/api': {
      target: 'http://localhost:8081',  // 用户服务
      changeOrigin: true
    }
  }
}
```

---

## 📁 项目结构

```
blog/
├── src/
│   ├── views/
│   │   ├── Home.vue          # 首页
│   │   ├── Blog.vue          # 博客列表
│   │   ├── BlogDetail.vue    # 文章详情
│   │   ├── UserProfile.vue   # 用户主页
│   │   └── user/             # 用户中心
│   ├── layouts/
│   │   ├── MainLayout.vue         # 主布局
│   │   └── UserCenterLayout.vue   # 用户中心布局
│   ├── stores/
│   │   └── user.js           # 只包含user store
│   └── utils/
│       └── api.js            # 用户API工具
├── vite.config.js
└── package.json
```

---

## ✨ 特点

- ✅ 完全独立的前端项目
- ✅ 只包含用户相关功能
- ✅ 独立的端口 (3001)
- ✅ 独立的token管理
- ✅ 与管理后台零耦合
