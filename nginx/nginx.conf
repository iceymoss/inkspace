# Nginx 反向代理配置

# 博客前端：is.iceymoss.com
server {
    listen 80;
    server_name is.iceymoss.com;

    # 代理到博客前端容器
    location / {
        proxy_pass http://blog-frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# 管理后台：is.admin.inkspace.iceymoss.com
server {
    listen 80;
    server_name is.admin.inkspace.iceymoss.com;

    # 代理到管理后台前端容器
    location / {
        proxy_pass http://admin-frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS 配置（如果有 SSL 证书，取消注释并配置）
# server {
#     listen 443 ssl http2;
#     server_name is.iceymoss.com;
# 
#     ssl_certificate /etc/nginx/ssl/is.iceymoss.com.cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/is.iceymoss.com.key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
# 
#     location / {
#         proxy_pass http://blog-frontend:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
# 
# server {
#     listen 443 ssl http2;
#     server_name is.admin.inkspace.iceymoss.com;
# 
#     ssl_certificate /etc/nginx/ssl/is.admin.inkspace.iceymoss.com.cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/is.admin.inkspace.iceymoss.com.key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
# 
#     location / {
#         proxy_pass http://admin-frontend:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

